"use client";
import {useEffect,useState} from "react";
import {useParams} from "next/navigation";
import * as api from "@/lib/api/rental-reports";

export default function PublicReportPage(){const {token}=useParams<{token:string}>();const [report,setReport]=useState<api.Report|null>(null),[notes,setNotes]=useState<Record<string,string>>({}),[saved,setSaved]=useState<Record<string,boolean>>({}),[error,setError]=useState("");useEffect(()=>{api.publicReport(token).then(r=>{setReport(r);setNotes(Object.fromEntries(r.items.map(i=>[i.id,i.notes||""])))}).catch(e=>setError(e.message))},[token]);async function save(itemId:string){try{await api.saveNote(token,itemId,notes[itemId]||"");setSaved(current=>({...current,[itemId]:true}))}catch(e){setError(e instanceof Error?e.message:"Could not save note")}}if(error&&!report)return <main className="mx-auto max-w-2xl p-8"><h1 className="text-2xl font-bold">Report unavailable</h1><p className="mt-3 text-[var(--ch-error-text)]">{error}</p></main>;if(!report)return <main className="p-8">Loading report…</main>;return <main className="mx-auto max-w-4xl p-4 sm:p-8"><header className="mb-8 border-b pb-5"><p className="text-xs uppercase tracking-[.2em] text-[var(--ch-text-muted)]">Office Hub</p><h1 className="mt-2 text-3xl font-bold">{report.title}</h1><p className="mt-2 text-sm text-[var(--ch-text-muted)]">Secure report · link expires {new Date(report.expires_at).toLocaleString()}</p></header>{error&&<p className="mb-4 rounded-xl bg-[var(--ch-error-bg)] p-3 text-[var(--ch-error-text)]">{error}</p>}{report.items.map(item=><article key={item.id} className="mb-8 break-inside-avoid rounded-2xl border bg-[var(--ch-surface)] p-5"><h2 className="text-2xl font-bold">{item.address} {item.unit_label||""}</h2><p className="text-sm text-[var(--ch-text-muted)]">{item.inspection_date} · {item.inspection_type}</p><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-xl border p-3"><span className="text-sm text-[var(--ch-text-muted)]">Front yard</span><strong className="block text-2xl">{item.front_yard_score??"—"}/10</strong><p>{item.front_yard_notes||"No notes"}</p></div><div className="rounded-xl border p-3"><span className="text-sm text-[var(--ch-text-muted)]">Back yard</span><strong className="block text-2xl">{item.back_yard_score??"—"}/10</strong><p>{item.back_yard_notes||"No notes"}</p></div></div><dl className="mt-4 space-y-2 text-sm"><div><dt className="font-bold">Building</dt><dd>{item.building_condition||"Not recorded"}{item.building_notes?` — ${item.building_notes}`:""}</dd></div><div><dt className="font-bold">Occupancy</dt><dd>{item.occupancy_flag||"Not recorded"}</dd></div><div><dt className="font-bold">General summary</dt><dd>{item.general_notes||"No additional observations"}</dd></div></dl>{item.photos.length?<div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3">{item.photos.map(photo=><img key={photo.id} src={api.absolutePhotoUrl(photo.url!)} alt={photo.caption||`Inspection photo for ${item.address}`} className="aspect-square w-full rounded-xl object-cover"/>)}</div>:<p className="mt-5 text-sm text-[var(--ch-text-muted)]">No inspection photos.</p>}<div className="mt-6 border-t pt-4"><label className="font-bold">Report notes<textarea value={notes[item.id]||""} onChange={e=>{setNotes(current=>({...current,[item.id]:e.target.value}));setSaved(current=>({...current,[item.id]:false}))}} className="mt-2 min-h-28 w-full rounded-xl border p-3" placeholder="Leave notes for this property…"/></label><button onClick={()=>save(item.id)} className="mt-2 rounded-xl bg-[var(--ch-accent)] px-5 py-3 font-bold text-white">{saved[item.id]?"Notes saved":"Submit notes"}</button></div></article>)}</main>}
