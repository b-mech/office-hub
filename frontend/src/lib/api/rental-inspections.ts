const BASE=process.env.NEXT_PUBLIC_API_URL||"http://127.0.0.1:8000"; const P="/api/rentals";
export type Photo={id:number;preview_url:string|null;caption:string|null}; export type Inspection={id:number;unit_id:number;inspection_type:string;inspection_date:string;inspector_name:string|null;front_yard_score:number|null;front_yard_notes:string|null;back_yard_score:number|null;back_yard_notes:string|null;building_condition:string|null;building_notes:string|null;occupancy_flag:string|null;general_notes:string|null;status:string;photos:Photo[]}; export type Unit={id:number;street_address:string;group_name:string|null;unit_label:string|null;last_inspection:{id:number;inspection_date:string;inspection_type:string;status:string}|null};
async function req<T>(path:string,o?:RequestInit):Promise<T>{const r=await fetch(BASE+path,{...o,cache:"no-store"});if(!r.ok){const b=await r.json().catch(()=>({}));throw new Error(b.detail||`API ${r.status}`)}return r.status===204?undefined as T:r.json()}
export const units=(q="")=>req<Unit[]>(`${P}/units?q=${encodeURIComponent(q)}`); export const history=(id:number)=>req<Inspection[]>(`${P}/units/${id}/inspections`); export const detail=(id:number)=>req<Inspection>(`${P}/inspections/${id}`); export const create=(unit_id:number)=>req<Inspection>(`${P}/inspections`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({unit_id})}); export const patch=(id:number,data:Partial<Inspection>)=>req<Inspection>(`${P}/inspections/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)}); export const submit=(id:number)=>req<Inspection>(`${P}/inspections/${id}/submit`,{method:"POST"}); export const upload=(id:number,file:File)=>{const b=new FormData();b.append("files",file);return req<Photo[]>(`${P}/inspections/${id}/photos`,{method:"POST",body:b})}; export const remove=(id:number,pid:number)=>req<void>(`${P}/inspections/${id}/photos/${pid}`,{method:"DELETE"});
